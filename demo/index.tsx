for (let i = 0; i < 4; i++) {
  let j = i
  /**
   * executed-code:disable
   * code-time:disable
   */
  while (j < 4) {
    j++
  }
}

/**
 * executed-code:disable
 * code-time:disable
 */
function foo() {
  console.info(1)
}

<button onClick={() => console.info(1)}></button>
